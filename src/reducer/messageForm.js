const initState = {
    "templateKey": "templateKey123",
    "receivers": []
}

export default function (state = initState, action) {
    switch (action.type) {
        default:
            return state;
    }
}